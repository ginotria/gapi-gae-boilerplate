moment.lang('en');

$.tools.dateinput.localize('en', {
    months: moment.months.join(","),
    shortMonths: moment.monthsShort.join(","),
    days: moment.weekdays.join(","),
    shortDays: moment.weekdaysShort.join(",")
});